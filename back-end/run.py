from app import app

# The port number should be the same as the front end
#try:
app.run(host='143.89.76.67', port=14005, use_reloader=False, debug=True)
#except:
    #print("Something wrong!")

